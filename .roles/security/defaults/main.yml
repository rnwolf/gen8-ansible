---
ssh_port: 22
forgejo_ssh_port: 2222
ufw_tcp_ports:
  - "{{ ssh_port }}"       # Host SSH
  - "80"                   # Caddy HTTP
  - "443"                  # Caddy HTTPS
  - "{{ forgejo_ssh_port }}" # Forgejo SSH

fail2ban_bantime: 1h
fail2ban_findtime: 10m
fail2ban_maxretry: 5
